# 2026-02-11 工作日志

## 身份
- NeFi (AI 小猫), 主人的个人助理

## 今日重点: n8n 工作流 1 调试 + 全链路跑通

### n8n 工作流 1 (SSRrZsmY1KanSY6K) 改动记录

1. **去掉 Claude 节点** — 内容直接从 Preview API 获取，不需要 AI 生成
2. **Search API Token 硬编码** — Bearer JWT 写在 headers 里，用 `sendHeaders` + `headerParameters` 格式
3. **Preview API 去掉认证** — 不需要 Bearer Token
4. **移除 Ping Google Sitemap** — Google 已废弃该端点 (404)
5. **移除 Ping Bing Sitemap** — 也报错 "invalid request"
6. **去掉 splitInBatches 循环** — 1 本书时行为异常，改为直接流式处理
7. **修复节点引用** — `遍历书籍` → `标记处理中`（因删除了 splitInBatches）
8. **修复 pm2 路径** — `/root/.npm/_npx/5f7878ce38f1eb13/node_modules/pm2/bin/pm2`
9. **Google Indexing API 启用** — 标记完成后提交 GSC
10. **Telegram 通知** — 替代 Slack

### 当前工作流数据流
```
获取待处理书籍(SSH) → 解析 → 检查是否有书
  → 有书 → 标记处理中 → Search API → 提取ID → 检查跳过
    → 找到 → Preview API → 组装数据 → 插入SQLite → 标记完成 → Google Indexing → 重建部署 → 统计 → Telegram
    → 没找到 → 标记跳过 → 重建部署
  → 没书 → Telegram 全部完成
```

### FizzRead API 关键信息
- Search API: 需要 Bearer Token + headers (x-app-version, x-locale, x-platform)
- Preview API: 不需要认证，只需 headers
- JWT Token 过期时间: 2027年 (exp: 1804694770)

### 封面图修复
- surrounded-by-idiots 的 cover_url 从假数据修复为真实 URL
- 真实封面来自 Search API 返回的 cover_url

### n8n API 踩坑
- jq 处理 JSON 时容易丢失节点 parameters（转义问题）
- 以后用 Node.js 脚本代替 jq 修改工作流
- PUT 更新时必须验证关键节点 parameters 完整性
- n8n HTTP Request 节点 headers 用 `sendHeaders` + `headerParameters.parameters[]` 格式

### 共享任务文件 (.claude/)
- TASK.md: Phase 1 完成，Phase 2 开始
- FR.OVH Claude Code 额外完成: BookCard 高度修复、书单索引页 /moment/lists
- PROGRESS.md + DECISIONS.md 已更新

### 当前状态
- 工作流 1 测试模式 (LIMIT 1)，全链路已跑通
- 94 本书种子全部 pending
- 12 本书已在 generated_books 中（手动插入的）
- 下一步: 恢复 LIMIT 20，激活定时任务

### 已完成（深夜）
- [x] 工作流简化：20 个节点 → 7 个（本地脚本替代 n8n 循环）
- [x] scripts/process-books.js + scripts/submit-gsc.js 由 FR.OVH Claude Code 完成
- [x] n8n cron bug 绕过：改用 webhook + OpenClaw cron 触发
- [x] Webhook: POST https://n8n.xiai.xyz/webhook/fizzread-seo-run
- [x] OpenClaw cron: 每天 02:00 UTC+8，job id: 7e67a5d7-58cb-42e9-8fba-907b637c19dc
- [x] 20 本/天，约 5 天跑完 94 本

### 待办
- [ ] 批量修复手动插入书籍的假 cover_url
- [ ] SEO 内容质量检查（英文、meta 格式）
- [ ] Lighthouse SEO ≥ 95
