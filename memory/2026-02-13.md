# 2026-02-13 工作日志

## n8n 每日任务
- 02:00 cron 自动触发 Workflow 1，处理 20 本新书（第 3 天，累计约 76 本）

## Sitemap 修复
- 发现分类页 `/moment/category/*` 未加入 sitemap.xml
- 修复 `app/moment/sitemap.xml/route.ts`，新增 `getAllCategories()` 调用
- sitemap URL 从 60 → 74（新增 14 个分类页）
- 14 个分类页通过 Google Indexing API 提交成功
- 书籍数据本身是实时的（56 本），不存在"没更新"问题
- 已同步到 ge.ovh

## 数据同步原则写入 ENV-GUIDE.md
- 主人提出开发/生产环境数据不一致问题
- 确立原则：**代码 ge.ovh→fr.ovh 单向，数据库 fr.ovh→ge.ovh 单向**
- 开发前必做：`scp fr.ovh:/data/fizzread-seo.db /data/fizzread-seo.db`
- 部署时只同步代码不动数据库
- schema 变更记录到 DECISIONS.md，由 NeFi 在生产执行
- ENV-GUIDE.md 已更新并同步三台机器

## 作者页开发（P0）
- 启动 ge.ovh Claude Code 开发作者页任务
- 遇到权限问题：debian 用户无法 cd /root/，已 `chown -R debian:debian` 修复
- Claude Code 启动确认提示需要选 "Yes, I accept"（Down + Enter）
- Claude Code 完成了大部分工作后超时被 kill（600s 不够）
- NeFi 接手完成剩余步骤：
  - 运行 backfill-authors.js → 53 个作者数据填充完毕
  - npm run build 成功，53 个作者页生成
  - pm2 restart fizzread-seo-dev
- 验证通过：作者页、面包屑、书籍页作者链接均正常

### 作者页功能清单
- `/moment/author/[slug]` — 53 个作者页
- 面包屑 4 级：Home > Moment > Authors > Author Name
- 作者头像（首字母圆形）+ H1 + 统计 + 短简介 + 书籍 grid + 完整 bio + CTA
- Person JSON-LD + BreadcrumbList
- 书籍详情页作者名改为链接 → `/moment/author/{slug}`

### AuthorSection 组件增强
- 主人要求 About the Author 区域的作者名和头像加链接跳转作者页
- 修改 `components/AuthorSection.tsx`：头像和名字都是 Link，hover 绿色高亮

### 作者重名处理
- 主人要求同名作者 slug 加数字后缀：`john-smith` → `john-smith-2`
- 修改 backfill-authors.js 和 process-books.js：
  - `authorSlug(name)` → `authorSlug(name, db)`
  - 先按 name 查已有 slug，slug 被不同作者占用时自动加 `-2` 后缀
- 前端改为从数据库查真实 slug：
  - 新增 `getAuthorSlugByName(name)` 函数到 lib/api.ts
  - 书籍详情页和 AuthorSection 组件都用此函数，不再前端硬算
- Build 通过，开发环境已更新

## 生产部署（作者页 + sitemap）
- 代码 ge.ovh → Mac 中转 → fr.ovh（ge.ovh debian 用户无法直接 scp 到 fr.ovh）
- 首次打包漏了作者页（git stash 导致），stash pop 后重新打包
- fr.ovh 需要 `npm install better-sqlite3` + `@types/better-sqlite3`
- 生产 authors 表为空，跑了 `backfill-authors.js`（53 个作者）
- Build 成功：81 → 134 页，sitemap 74 → 127 URL
- 作者页已提交 GSC（待做）

## Claude Code 启动失败排查
- 症状：启动后 30 秒无输出直接 SessionEnd，没有任何 LLM API 调用
- 网络正常（api.anthropic.com 0.15s 响应）
- MCP 初始化正常（0.2s 获取 0 servers）
- 账号：Pro + Extra Usage，上次会话消耗 $2.6（Opus 4.6）
- 结论：大概率是 Pro 账号用量限额暂时耗尽，等几小时重置
- Claude Code 路径：`/home/debian/.local/bin/claude`（v2.1.41）

## Header 移动端菜单修复
- 汉堡菜单从静态按钮改为可交互（useState 控制展开/收起）
- 展开后显示 Moments、Top Lists、Try FizzRead Free
- 点击链接或 X 按钮自动收起
- ge.ovh Claude Code 启动失败（无输出），NeFi 直接改的代码
- 已部署到生产

## Claude Code 进度通知机制
- 部署 `notify-nefi` 脚本到 ge.ovh + fr.ovh（/usr/local/bin/）
- 通过 Telegram Bot API 发消息到主人 chat（1149648904）
- CLAUDE.md 新增「📡 进度通知」规则：任务完成/失败/卡住必须调用
- 长任务中间至少通知一次进度
- Gateway 是 loopback 模式，无法从远程 curl，所以用 Telegram 替代

## SSH 互通配置
- ge.ovh ↔ fr.ovh 配置了 debian 用户 ed25519 密钥互信
- 两台机器 `~/.ssh/config` 已配好 alias，直接 `ssh fr.ovh` / `ssh ge.ovh`
- 部署不再需要 Mac 中转，ge.ovh 可直接 scp 到 fr.ovh

## Backlink 策略
- 创建了 `fizzread-backlink-strategy-2026-02-13.md`
- P0: Product Hunt launch + Reddit/Medium 内容营销
- 竞品分析：BeFreed 的名人书单页 5000-10000 字，内容很厚

## 名人书单项目（Celebrity Book Lists）

### 第一版（已废弃）
- 从已有 56 本书里匹配 10 个名人各 10 本 → 主人否决
- 问题：应该先确定名人真实推荐，再搜 API 入库新书，扩充书库

### 第二版（正确流程）
- 流程：确定名人 → 搜索真实推荐书 → FizzRead Search API 查书 → 新书入库 → 生成详情页 → 书单页引用
- 10 个名人：Bill Gates, Elon Musk, Warren Buffett, Oprah Winfrey, Mark Zuckerberg, Naval Ravikant, Tim Ferriss, Barack Obama, Jordan Peterson, Joe Rogan
- 书单来源已验证：GatesNotes, Twitter, Berkshire letters, Oprah.com, Facebook "A Year of Books", Naval podcast, Tim Ferriss Show, Obama annual lists, jordanbpeterson.com, JRE episodes
- 总计 72 本去重书籍：21 本已有 ✅，51 本需新生成
- 43 本新加入 book_seeds（priority=1），8 本之前已在队列
- 脚本：`scripts/build-celebrity-lists.js`（搜索 API + 入库）
- 结果文件：`/tmp/celebrity-lists-result.json`（10 个书单的 slug 映射）

### book_seeds 表结构
- 没有 book_id 列！只有 slug, title, author, search_volume, keyword_difficulty, priority, category, keywords, status
- 新书用 priority=1 标记优先处理

### 书单页 SEO 设计
- 每本书：封面 + intro 摘要 + Key Takeaways + "Read full summary" 内链 + Jump to 导航
- 4 级面包屑：Home > Moment > Top Lists > List Name
- JSON-LD structured data
- 书单数据目前硬编码在 `lib/api.ts` 的 `bookLists` 数组（12 个：10 名人 + 2 主题）
- 计划迁移到 `celebrity_lists` DB 表

### 第三版（10 个新名人，已完成）
- 新增 10 个名人：Reese Witherspoon, Emma Watson, LeBron James, Richard Branson, Ryan Holiday, James Clear, Brené Brown, Malcolm Gladwell, Sam Altman, Sheryl Sandberg
- 脚本 `build-celebrity-lists-v2.js` 在 ge.ovh 运行，全部 10/10 匹配
- 35 本新书 seed 同步到 fr.ovh，Workflow 1 触发生成，全部完成
- 总计：388 本书，348 个作者，22 个书单（20 名人 + 2 主题）
- lib/api.ts 中 bookLists 数组更新为 22 个条目
- 结果文件：`/tmp/celebrity-lists-v2.json`

### 书单全部已部署 ✅
- 22 个书单页上线，sitemap 803 个 URL
- 分页改造完成（首页、分类页、书单页都支持省略号分页）
- 书单标题多样化（每个书单独特标题）

## Header CTA 跳转修改
- "Try FizzRead Free" 按钮从 fizzread.ai 首页改为 App Store 下载链接
- 带 UTM 追踪：`utm_source=seo&utm_medium=header&utm_campaign=try-free`
- 已部署生产 + 开发环境

## zero-to-one 中文数据修复
- FizzRead API 返回的 about 字段是中文（intro、author_bio、faqs[0]、faqs[2]）
- 手动替换为英文内容，重新 build 部署
- 全库扫描确认只有这一本有中文问题

## n8n Workflow 调整
- Workflow 1 新增 n8n Schedule Trigger（每天 03:00 UTC+8）→ 主人要求删除
- 保留 OpenClaw cron job（02:00 UTC+8）作为唯一定时触发
- Workflow 1 现在只有 Webhook 触发入口

## GSC 批量提交方案重设计
- 新脚本 `scripts/submit-gsc-batch.js`（替代旧的 submit-gsc.js）
- 新建 `gsc_submissions` 表记录已提交 URL，避免重复
- 支持 --type books/authors/lists/all、--limit N、--status
- 覆盖所有页面类型：书籍、作者、书单、分类、首页
- Google Indexing API 每日配额约 200 个请求
- 今日提交 186 个书籍页，剩余 617 个 URL
- 预计 4 天全部提交完
- Workflow 2（GSC 每日提交）已更新为新脚本，inactive 待主人激活

## 待处理任务
- [ ] Workflow 2（GSC 每日提交）— 主人检查后激活
- [ ] Backlink 策略执行 — Product Hunt launch、Reddit/Medium 内容营销
- [ ] 书单数据迁移到数据库（目前硬编码 lib/api.ts）
- [ ] 主题书单扩充（目前 2 个，可扩到 10 个）
- [ ] ~~Lists 首页双区布局~~ — 主人取消此任务

## 技术备忘
- Claude Code 超时 600s 不够大任务，下次考虑 900s+
- Claude Code 权限确认：需要 Down + Enter 选 "Yes, I accept"
- n8n Workflow 1 每天 02:00 UTC+8 由 OpenClaw cron 触发，webhook: POST https://n8n.xiai.xyz/webhook/fizzread-seo-run
- 388 本书全部完成，0 pending
- FizzRead Search API: GET www.fizzread.ai/api/v1/book/search?query=xxx&mode=simple（需 Bearer token）
- sed 替换代码块要小心：之前误删了 comparisons 数组和 getBookList/getAllListSlugs 函数
- GSC 提交状态表：gsc_submissions（url, type, slug, submitted_at, status）
- Google Indexing API 实际每日配额约 186-200 个
